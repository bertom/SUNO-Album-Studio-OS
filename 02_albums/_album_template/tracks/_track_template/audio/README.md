# Audio

Store SUNO outputs and finals here.

**Naming:** `runNNN_take_x.wav` for runs; `final.wav` or `final_master.wav` for selected version.

Link every file in `suno/suno_runs.md` and `analysis/listening_notes.md`.

Binary files may be gitignored in future — paths in Markdown remain truth.
